name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "Hello Github Actions"
      - name: node npm versions check
        run: |
          node -v
          npm -v